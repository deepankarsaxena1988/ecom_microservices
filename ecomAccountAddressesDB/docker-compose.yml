services:
  ecom-account-addresses-db:
    build: .
    container_name: ecom-account-addresses-db
    ports:
      - "3312:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: ecomAccountAddressesDB
      MYSQL_USER: ecomUser
      MYSQL_PASSWORD: root123
    volumes:
      - ecom_account_addresses_db_data:/var/lib/mysql

volumes:
  ecom_account_addresses_db_data:
